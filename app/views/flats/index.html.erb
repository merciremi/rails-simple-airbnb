<div class="container">
  <h1>Flats</h1>
  <div class="row">
    <% @flats.each do |flat| %>
      <div class="col-xs-4">
        <div class="flat-card">
          <div class="card-info">
            <h2><%= flat.name %></h2>
            <p>🗺️ <%=flat.address%></p>
            <p>✍️ <%=flat.description%></p>
            <p>💰 $<%=flat.price_per_night%></p>
            <p>👩‍👩‍👧‍👦 <%=flat.number_of_guests%> guests</p>
          </div>
          <div class="bottom-links"><%= link_to 'Show', flat %> /
            <%= link_to 'Edit', edit_flat_path(flat) %> /
            <%= link_to 'Destroy', flat, method: :delete, data: { confirm: 'Are you sure?' } %>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <hr>

  <%= link_to 'New Flat', new_flat_path %>
</div>
